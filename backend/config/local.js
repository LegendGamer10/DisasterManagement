/**
 * Local configuration for environments where .env is not convenient.
 *
 * Option 1 (recommended): Keep this empty and use OPENAI_API_KEY in .env.
 * Option 2: Paste your key below for quick local testing.
 */
module.exports = {
  OPENAI_API_KEY: ''
};
